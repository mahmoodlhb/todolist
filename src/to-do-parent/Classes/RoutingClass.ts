import {CardModel} from "../interface/CardModel";

export class RoutingClass{
  public static doingList:Array<CardModel>=new Array<CardModel>();
  public static doneList:Array<CardModel>=new Array<CardModel>();

}
